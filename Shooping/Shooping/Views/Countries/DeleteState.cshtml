@model Shooping.Data.Entities.State

@{
    ViewData["Title"] = "Delete";
}

<h1>Borrar</h1>

<h3>Se esta borrando el Departamento/Estado, ¿Desea continuar con el borrado?</h3>
<div>

    <patial name="_StateDetails" /> <!--Hace llamado de la Vista parcial-->
</div>
<div>
    <form asp-action="DeleteState">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Borrar" class="btn btn-outline-danger" />
        <a asp-action="Details" asp-route-id="@Model?.Country.Id" class="btn btn-outline-success">Regresar</a>
    </form>
</div>
